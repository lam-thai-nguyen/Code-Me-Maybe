#include <cmath>
#include <iostream>
using namespace std;

int main()
{
    // Finding max(), min() 
    cout << max(1, 4) << endl; 
    // <cmath> gives sqrt, round, log
    cout << sqrt(64) << endl;

    return 0;
}